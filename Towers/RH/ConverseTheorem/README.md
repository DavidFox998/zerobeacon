# Towers/RH/ConverseTheorem

Converse theorem — input for Langlands transfer `LanglandsTransfer L_fn`.

Provides `Converse.lean` — shows `L_fn` is automorphic. Used by `P5_BSD_RH_closure_CLOSED` via `grh_to_rh_descent`.
